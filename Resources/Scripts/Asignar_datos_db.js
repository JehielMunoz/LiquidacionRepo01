function AsignarId(nam)
{   
    
    var name = nam.value; // nombre a buscar el index 
    var name_index = nombre.indexOf(name); // busca el index //
    var ide = id[name_index]; // devuelve la id.
    document.getElementById("id_nombre").value= ide;
    alert(ide);
    
    
}